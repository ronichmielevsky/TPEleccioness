@{
    ViewData["Title"] = "Home Page";
}
<div class="container">
@foreach (Partido item in ViewBag.listPartido)
{
<div class="card">
    <img class="card-img-top" src="@item.Logo" alt="Card image cap">
    <div class="card-body">
      <h2 class="card-title">@item.Nombre</h2>
    </div>
        <div class="card-footer">
      <small class="text-muted"><a href='@Url.Action("VerDetallePartido","Home", new {idPartido=@item.IDPartido})'><button>ver el detalle del partido.</button></a></small>
    </div>
  </div>
}
</div>